import React, { Component } from 'react';

class RecoverPassword extends Component {
    render() {
        return (
            <div className="is-title">Recover Password</div>
        );
    }
}

export default RecoverPassword;