import React, { Component } from 'react';

class Login extends Component {
    render() {
        return (
            <div className="is-title">Sign In</div>
        );
    }
}

export default Login;